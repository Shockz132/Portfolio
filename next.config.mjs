/** @type {import('next').NextConfig} */

const repo = "Portfolio";
const assetPrefix = `/${repo}/`;
const basePath =`/${repo}`


const nextConfig = {
    output: 'export',
};

export default nextConfig;
